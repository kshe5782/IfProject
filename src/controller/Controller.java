package controller;

import Model.Model;
import javax.swing.JOptionPane;

public class Controller 
{
{
}
	//slash star star starts java doc
	/**
	 * Builds instance of Controller
	 */
	public Controller()
	{

}
	/**
	 * Program starts calling methods to run program
	 */
	public void start()
{
		loopy();
}
	
	private void loopy()
	{

		//define the variable before the loop
		boolean isDone = false;
		int count = 0;
		
		while (!isDone);  //Test the variable
			}
	JOptionPane.showMessageDialog(null, "I am basically Monky");
	//Eventually change the loop variable 
	count++;
	if(count >= 10)
	{
		isDone = true; //update variable
	}
	}
{
	for (int loop = 0; loop < 30; loop += 2 )
{
	JOptionPane.showMessageDialog(null, "The loop value is : " + loop);
			}
	
	
}}